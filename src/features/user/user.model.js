export default class UserModel {
  constructor(name, password, about, email, role) {
    (this.name = name),
      (this.password = password),
      (this.about = about),
      (this.email = email),
      (this.role = role);
  }
}
